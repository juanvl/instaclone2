import styled from 'styled-components/native';
import {
  Container as ContainerSuper,
  Input as InputSuper,
  Button as ButtonSuper,
  ButtonText as ButtonTextSuper,
} from '~screens/Login/styles';

export const Container = styled(ContainerSuper)``;

export const Input = styled(InputSuper)`
  padding-left: 15px;
`;

export const Button = styled(ButtonSuper)``;

export const ButtonText = styled(ButtonTextSuper)``;
